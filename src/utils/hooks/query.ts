
export default function temp() {

}
// import {useQuery} from 'react-query';
// import {relationshipForms} from '../store/questions';

// export function useFormQuery(formId: string) {
//   const {isLoading, error, data} = useQuery(formId, () => {
//     return relationshipForms[formId];
//     // return new Promise<FormData>(() => relationshipForms[formId]).then((data) => data);
//   });

//   return [isLoading, error, data] as const;
// }
